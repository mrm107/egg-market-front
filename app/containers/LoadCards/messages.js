/*
 * LoadCards Messages
 *
 * This contains all the text for the LoadCards component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.LoadCards.header',
    defaultMessage: 'This is LoadCards container !',
  },
});
